[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sad-mcp"
version = "0.1.0"
description = "Suspicious Activity Detection MCP service for the Central QSR Agent."
requires-python = ">=3.11"
dependencies = [
    "mcp-service-sdk[mcp] @ git+https://github.com/sachinkaushik/edge-ai-libraries.git@mcp#subdirectory=libraries/mcp-service-sdk",
]

[project.scripts]
sad-mcp = "main:main"

[tool.setuptools]
package-dir = {"" = "src"}
py-modules = ["config", "events", "models", "queries", "tools", "main"]

[tool.pytest.ini_options]
pythonpath = ["src"]
