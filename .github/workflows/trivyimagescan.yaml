# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: trivy_image_scan

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main
permissions:
  contents: read

jobs:
    trivy_image_scan_storewide-lossprevention:
      permissions:
        contents: read # for actions/checkout to fetch code
        security-events: write # for github/codeql-action/upload-sarif to upload SARIF results
        actions: read # only required for a private repository by github/codeql-action/upload-sarif to get the Action run status
      name: trivy_image_scan
      runs-on: "ubuntu-latest"
      steps:
        - name: Checkout code
          uses: actions/checkout@1e31de5234b9f8995739874a8ce0492dc87873e2
        - name: Read TAG from Makefile
          shell: bash
          run: |
            VERSION="$(grep -m1 '^TAG' suspicious-activity-detection/Makefile | sed 's/TAG.*=\s*//' | tr -d '\n\r' | xargs)"
            echo "VERSION=$VERSION" >> "$GITHUB_ENV"
            echo "Using VERSION=$VERSION"
            
        - name: Build suspicious-activity-detection vss-recall-bridge image
          run: |
            cd suspicious-activity-detection
            docker build -t intel/swlp-vss-recall-bridge:${{ env.VERSION }} -f vss-recall-bridge/Dockerfile vss-recall-bridge/

        - name: Build suspicious-activity-detection intel/swlp-suspicious-ui image
          run: |
            cd suspicious-activity-detection
            docker build -t intel/swlp-suspicious-ui:${{ env.VERSION }} -f ui/Dockerfile ui/  

        - name: Run Trivy vulnerability scanner on swlp-vss-recall-bridge
          uses: aquasecurity/trivy-action@57a97c7e7821a5776cebc9bb87c984fa69cba8f1
          with:
            image-ref: "intel/swlp-vss-recall-bridge:${{ env.VERSION }}"
            format: 'table'
            output: 'trivy-results-swlp-vss-recall-bridge.txt'
            severity: 'CRITICAL,HIGH'

        - name: Run Trivy vulnerability scanner on swlp-suspicious-ui
          uses: aquasecurity/trivy-action@57a97c7e7821a5776cebc9bb87c984fa69cba8f1
          with:
            image-ref: "intel/swlp-suspicious-ui:${{ env.VERSION }}"
            format: 'table'
            output: 'trivy-results-swlp-suspicious-ui.txt'
            severity: 'CRITICAL,HIGH'
            
        - name: Upload Trivy scan results
          uses: actions/upload-artifact@v4
          with:
            name: trivy-suspicious-activity-detection-report
            path: |
              trivy-results-swlp-vss-recall-bridge.txt
              trivy-results-swlp-suspicious-ui.txt
